<p>Hello <%= @user.default_profile.name %>!</p>

<p>Follow this link to reset your password:</p>

<p><%= link_to url_for(:controller => 'toolbawks/auth', :action => 'reset_password', :token => @token, :only_path => false), url_for(:controller => 'toolbawks/auth', :action => 'reset_password', :token => @token, :only_path => false) %></p>